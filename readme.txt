这是一个移植了freertos的stm32示例工程。

该工程演示了freertos系统在stm32上是如何运行的。

本工程实现了以下基本功能：
1、定时器定时功能
2、通用定时器实现输出pwm信号
3、高级定时器实现pwm互补输出，并插入死区时间，设置了刹车功能
4、通用定时器实现pwm输入捕获（两种方式，input capture mode和pwm input mode）
5、定时器定时产生触发信号，触发ADC采样，adc通道转换完成后使用dma把数据搬运到指定缓存
6、打印出所有任务的状态信息
